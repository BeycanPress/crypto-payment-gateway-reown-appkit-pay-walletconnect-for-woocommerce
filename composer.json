{
    "scripts": {
        "phpcs": "phpcs --standard=phpcs.xml .",
        "phpcbf": "phpcbf --standard=phpcs.xml .",
        "install-phpcs": "composer config --global --no-plugins allow-plugins.dealerdirect/phpcodesniffer-composer-installer true && composer require --dev squizlabs/php_codesniffer=* slevomat/coding-standard"
    },
    "autoload": {
        "psr-4": {
            "BeycanPress\\ReownPaymentGateway\\": "app"
        }
    },
    "require": {
        "beycanpress/currency-converter": "^0.1.2",
        "multiplechain/evm-chains": "^0.1.24"
    }
}
